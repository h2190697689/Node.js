// util.inherits (实现继承)

// util.inspect  (将任意对象转化为字符串)

// util.isArray(object)
// const util= require("util");
// console.log(util.isArray([1,2,3]));

// util.isRegExp(object)

// util.isDate(object)

// util.isError(object)